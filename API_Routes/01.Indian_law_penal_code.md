# Indian_law_penal

## CRUD Routes

```http
GET /laws
GET /laws/:id
POST /laws
PUT /laws/:id
PATCH /laws/:id
DELETE /laws/:id
```

---

## Pagination Routes

```http
GET /laws?page=1&limit=10
GET /laws/scroll?cursor=20&limit=10
GET /laws/offset?skip=0&limit=20
GET /laws?page=1&limit=10&sort=views
GET /laws?page=2&limit=50
```

---

## Filtering Routes

```http
GET /laws/chapter/1
GET /laws/filter?lawName=IPC
GET /laws/filter?Section=302
GET /laws/filter?isActive=true
GET /laws/bookmarked
GET /laws/filter?chapters=1,2,3
GET /laws/filter?views_gt=100
GET /laws/filter?views_lt=50
GET /laws/filter?createdAfter=2025-01-01
GET /laws/filter?updatedBefore=2026-01-01
GET /laws/filter?chapter_title=introduction
GET /laws/filter?tags=crime
GET /laws/filter?lawName=IPC&chapter=2
GET /laws/filter?bookmarked=true&views_gt=10
GET /laws/filter?section_title=theft
```

---

## Search Routes

```http
GET /laws/search?title=murder
GET /laws/search?desc=punishment
GET /laws/fulltext?q=theft
GET /laws/regex?q=fraud
GET /laws/tags/crime
GET /laws/search?q=kidnapping
GET /laws/search?q=cybercrime
GET /laws/search?chapterTitle=general
GET /laws/search?startsWith=pun
GET /laws/search?endsWith=ment
```

---

## Sorting Routes

```http
GET /laws/sort/chapter-asc
GET /laws/sort/chapter-desc
GET /laws/sort/views
GET /laws/sort/title
GET /laws/sort/createdAt-desc
GET /laws/sort/updatedAt-asc
GET /laws/sort/section-asc
GET /laws/sort/section-desc
```

---

## Aggregation Routes

```http
GET /laws/stats/count
GET /laws/stats/chapters
GET /laws/stats/most-viewed
GET /laws/stats/avg-views
GET /laws/stats/law-types
GET /laws/stats/bookmarked-count
GET /laws/stats/max-views
GET /laws/stats/min-views
GET /laws/stats/chapters-count
GET /laws/stats/tags-count
```

---

## Advanced Routes

```http
PATCH /laws/soft-delete/:id
PATCH /laws/restore/:id
POST /laws/bulk-insert
DELETE /laws/bulk-delete
PATCH /laws/bulk-update/views
PATCH /laws/views/:id
PATCH /laws/tags/:id
PATCH /laws/remove-tag/:id
GET /laws/random?limit=5
GET /laws/export/csv
GET /laws/export/excel
POST /laws/import-json
GET /laws/chapter-summary
GET /laws/duplicates
GET /laws/longest-descriptions
GET /laws/shortest-titles
PATCH /laws/bookmark/:id
PATCH /laws/unbookmark/:id
DELETE /laws/delete-all
GET /laws/recently-added
GET /laws/trending
GET /laws/top-rated
GET /laws/least-viewed
GET /laws/active
GET /laws/inactive
```

---

## Authentication Practice Routes

```http
POST /auth/register
POST /auth/login
POST /auth/logout
POST /auth/refresh-token
GET /auth/profile
PATCH /auth/profile
DELETE /auth/profile
POST /auth/forgot-password
POST /auth/reset-password
GET /auth/users
```

---

## Admin Practice Routes

```http
GET /admin/dashboard
GET /admin/users
PATCH /admin/user/:id
DELETE /admin/user/:id
GET /admin/reports
GET /admin/analytics
PATCH /admin/law/:id
DELETE /admin/law/:id
GET /admin/logs
GET /admin/system-health
```

